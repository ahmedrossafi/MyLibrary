<!-- Ce texte sera importé de index.ejs à la partie body de layout.ejs-->
<h2>Recently Added</h2> 
<div>
    <% books.forEach(book => { %>
        <img height="150" width="100" src="<%= book.coverImagePath %>">
    <% }) %>
</div>